import 'package:flutter/material.dart';

class Detaildorm extends StatelessWidget {
  const Detaildorm({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('รายละเอียดหอพัก'),
        
      ),
    );
  }
}